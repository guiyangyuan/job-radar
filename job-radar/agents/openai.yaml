interface:
  display_name: "Job Radar"
  short_description: "Discover matching jobs and track applications locally"
  default_prompt: "Use $job-radar to build my job-search profile, find matching roles, and maintain my local application dashboard."
